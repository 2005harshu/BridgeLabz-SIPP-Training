CLASS Student
    name
    marks

FUNCTION mergeSort(list)
    IF size(list) <= 1
        RETURN list
    mid = size(list) / 2
    left = mergeSort(first half)
    right = mergeSort(second half)
    RETURN merge(left, right)

FUNCTION merge(left, right)
    result = empty list
    WHILE left and right not empty
        IF left[0].marks > right[0].marks
            add left[0] to result
            remove left[0]
        ELSE IF marks equal AND left[0].name < right[0].name
            add left[0] to result
            remove left[0]
        ELSE
            add right[0] to result
            remove right[0]
    add remaining elements
    RETURN result

MAIN
    list = [(Amit,92), (Bhavna,92), (Kiran,85), (Dinesh,78), (Charu,92)]
    sortedList = mergeSort(list)
    PRINT sortedList
